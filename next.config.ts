import type { NextConfig } from "next";

const nextConfig: NextConfig = {
 images: {
    domains: ['res.cloudinary.com', 'www.tobacconepal.com'], // add any external image hosts here
  },
};

export default nextConfig;
